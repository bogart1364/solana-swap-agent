# Optional: your own Solana RPC endpoint (QuickNode, Helius, Triton, Alchemy, etc.)
# Without this, the app falls back to the public mainnet-beta endpoint, which is
# rate limited and not reliable enough for real trading.
NEXT_PUBLIC_SOLANA_RPC_URL=
